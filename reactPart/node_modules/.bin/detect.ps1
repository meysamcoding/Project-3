#!/usr/bin/env pwsh
$basedir=Split-Path $MyInvocation.MyCommand.Definition -Parent

$exe=""
if ($PSVersionTable.PSVersion -lt "6.0" -or $IsWindows) {
  # Fix case when both the Windows and Linux builds of Node
  # are installed in the same directory
  $exe=".exe"
}
$ret=0
if (Test-Path "$basedir/node$exe") {
<<<<<<< HEAD
  & "$basedir/node$exe"  "$basedir/../detect-port/bin/detect-port" $args
  $ret=$LASTEXITCODE
} else {
  & "node$exe"  "$basedir/../detect-port/bin/detect-port" $args
=======
  & "$basedir/node$exe"  "$basedir/../detect-port-alt/bin/detect-port" $args
  $ret=$LASTEXITCODE
} else {
  & "node$exe"  "$basedir/../detect-port-alt/bin/detect-port" $args
>>>>>>> refs/remotes/origin/master
  $ret=$LASTEXITCODE
}
exit $ret
